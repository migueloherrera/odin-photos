<div class="flickr">
  <ul>
    <% photos.each do |p| %>
			<% info = flickr.photos.getInfo(photo_id: p.id) %>
			<%= link_to image_tag(FlickRaw.url_n(info), class: "flickr_img"), FlickRaw.url_photopage(info) %>
		<% end %>
		
  </ul>
</div>
